type: object
required:
  - error
properties:
  error:
    type: object
    required:
      - code
      - message
    properties:
      code:
        type: string
        enum:
          - NOT_FOUND
        description: |
          Внутренние коды ошибок:
          - NOT_FOUND - значения нет 
      message:
        type: string
        description: Описание ошибки