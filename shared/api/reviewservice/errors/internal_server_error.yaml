type: object
required:
  - error
properties:
  error:
    type: object
    required:
      - code
      - message
    properties:
      code:
        type: string
        enum:
          - INTERNAL_ERROR
        description: |
          Внутренние коды ошибок:
          - INTERNAL_ERROR - ошибка в работе сервиса
      message:
        type: string
        description: Описание ошибки